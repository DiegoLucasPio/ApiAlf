
  # API Integration

  This is a code bundle for API Integration. The original project is available at https://www.figma.com/design/oGeRlxvvTOwqYM7E774PXv/API-Integration.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  